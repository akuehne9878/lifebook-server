<mvc:View xmlns:mvc="sap.ui.core.mvc" xmlns="sap.m" xmlns:codeeditor="sap.ui.codeeditor" xmlns:core="sap.ui.core" xmlns:layout="sap.ui.layout" xmlns:uxap="sap.uxap" controllerName="lifebook.view.main.detail.Page1Header">
			<OverflowToolbar>
			   <Button type="Transparent" icon="sap-icon://menu"  press="onShowMaster" visible="{=(${device>/system/tablet} &amp;&amp; ${device>/orientation/portrait}) || ${device>/system/phone}}" />
            <Title text="{currPage>/title}" />
				<ToolbarSpacer />

            <Button type="Transparent" icon="sap-icon://add-document" text="Neue Seite" press="onShowNew" visible="{=${currPage>/title} !== undefined}" />
            <Button type="Transparent" icon="sap-icon://edit" text="Umbenennen" press="onShowEdit" visible="{=${currPage>/title} !== undefined}"/>
            <Button type="Transparent" icon="sap-icon://upload" text="Upload" press="onShowUpload" visible="{=${currPage>/title} !== undefined}" />
            <Button type="Transparent" icon="sap-icon://copy" text="Kopieren" press="onShowCopy" visible="{=${currPage>/title} !== undefined}" />
            <Button type="Transparent" icon="sap-icon://action" text="Verschieben" press="onShowMove" visible="{=${currPage>/title} !== undefined}" />
            <Button type="Transparent" icon="sap-icon://delete" text="Löschen" press="onDelete" visible="{=${currPage>/title} !== undefined}" />
 
         </OverflowToolbar>
</mvc:View>